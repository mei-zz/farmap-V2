version: '3.8'

services:
  tree-analyzer:
    build: .
    ports:
      - "5000:5000"
    environment:
      - API_KEYS=${API_KEYS:-}
      - HUNYUAN_API_KEY=${HUNYUAN_API_KEY:-}
      - PORT=5000
    restart: unless-stopped
